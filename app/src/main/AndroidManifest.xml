<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grademojo.add_teacher">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Again_New_Login_Page"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Teacher_select_yes" />
        <activity android:name=".New_Login_Signup_Page" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Teacher" />
        <activity android:name=".Teacher_Select_Yes_Again" />
        <activity android:name=".Teacher_Select_no" />
        <activity android:name=".Main2Activity"/>
    </application>

</manifest>